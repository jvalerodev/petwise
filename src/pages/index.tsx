import Layout from '@/components/layout/Layout';
import TopCards from '@/components/TopCards';

const Home = () => {
  return (
    <Layout>
      <TopCards />
    </Layout>
  );
};

export default Home;
